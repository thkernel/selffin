$("ul.errors").html("");
<% if @bank.errors.any? %>
  <% @bank.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".banks").html("<%= escape_javascript(render 'index') %>");
  $("#bank-modal").modal("hide");
 
<% end %>