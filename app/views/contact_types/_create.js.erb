$("ul.errors").html("");
<% if @contact_type.errors.any? %>
  <% @contact_type.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".contact-types").html("<%= escape_javascript(render 'index') %>");
  $("#contact-type-modal").modal("hide");
 
<% end %>