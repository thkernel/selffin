$("ul.errors").html("");
<% if @expense_category.errors.any? %>
  <% @expense_category.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".expense-categories").html("<%= escape_javascript(render 'index') %>");
  $("#expense-category-modal").modal("hide");
 
<% end %>