<%= form_with(model: income_expense, local: true) do |form| %>
  <% if income_expense.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(income_expense.errors.count, "error") %> prohibited this income_expense from being saved:</h2>

      <ul>
      <% income_expense.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :uid %>
    <%= form.text_field :uid %>
  </div>

  <div class="field">
    <%= form.label :income_id %>
    <%= form.text_field :income_id %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
