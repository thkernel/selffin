$("ul.errors").html("");
<% if @poll_category.errors.any? %>
  <% @poll_category.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".poll-categories").html("<%= escape_javascript(render 'index') %>");
  $("#poll-category-modal").modal("hide");
 
<% end %>