$("ul.errors").html("");
<% if @poll.errors.any? %>
  <% @poll.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".polls").html("<%= escape_javascript(render 'index') %>");
  $("#poll-modal").modal("hide");
 
<% end %>